# 违规案例记录

> 本文件位于 `.trae/rulelog.md`，加载上下文时必读（仅本文件）。
> 记录用户点名的违规案例，避免重复犯错。
> 规则变更历史见 `rulelog_history.md`（按需读取，不自动加载）。

---

| 日期 | 项目 | 违规描述 | 纠正措施 |
|------|------|---------|---------|
| 2026-04-27 | project1 | 1. 构思阶段：构思方案直接写入chapter_01.md文件，状态错误更新为"草稿"（应只在对话中输出，状态保持"构思"）<br>2. 草稿阶段：用户说"写草稿"时直接写入完整正文到文件，状态错误更新为"正文"（应只在对话中输出草稿，状态保持"草稿"） | 1. 构思方案只在对话中输出，不写入文件<br>2. 草稿只在对话中输出，标注[草稿待确认]<br>3. 正文只在对话中输出，标注[正文待确认]<br>4. 只有用户确认存档后才写入文件 |