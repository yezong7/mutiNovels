{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/mnt/g/tmpRepo/mutiNovels"
      ],
      "env": {},
      "_comment": "原配置 - 另一台电脑 (Windows G盘路径)"
    },
    "filesystem-local": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/home/elio/Documents/novel/mutiNovels"
      ],
      "env": {},
      "_comment": "本地配置 - 当前电脑 Linux路径"
    },
    "memorymesh": {
      "type": "stdio",
      "command": "memorymesh",
      "args": [],
      "env": {},
      "_comment": "原配置 - 另一台电脑 (需先运行 memorymesh start)"
    },
    "memorymesh-npx": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@mseep/memorymesh"
      ],
      "env": {},
      "_comment": "本地配置 - 直接运行无需启动stack"
    }
  }
}